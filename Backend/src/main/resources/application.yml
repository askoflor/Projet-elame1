spring:
  application:
    name: backend
  profiles:
    active: dev
  jpa:
    open-in-view: false

server:
  port: 8080

app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:*}

logging:
  level:
    root: INFO
