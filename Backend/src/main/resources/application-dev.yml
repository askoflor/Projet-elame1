spring:
  datasource:
    url: jdbc:mysql://localhost:3307/backend_db
    username: assako
    password: Passe123!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

springdoc:
  swagger-ui:
    enabled: true

logging:
  level:
    com.renkotechnologie.backend: DEBUG
